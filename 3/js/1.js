const range = 20; // максимальное значение
const count = 10;      // кол-во требуемых чисел
let m = {}; //массив m изначально пустой,
let a = [];
for (let i = 0; i < count; ++i) { //сложение перед тем как выполнять, предсложение
  let r = Math.floor(Math.random() * (range - i));  //floor округление вниз (6,9=6), округл. рандомное значение [0-20)
  a.push();//если значение r наход-ся в m, что будет при выполнении ?, 
 
}
alert(a.join("-"));